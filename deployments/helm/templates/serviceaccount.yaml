{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "sora.serviceAccountName" . }}
  labels:
    {{- include "sora.labels" . | nindent 4 }}
{{- end }}
