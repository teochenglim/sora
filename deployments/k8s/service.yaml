apiVersion: v1
kind: Service
metadata:
  name: sora
  labels:
    app: sora
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8080"
    prometheus.io/path: "/metrics"
spec:
  selector:
    app: sora
  ports:
    - name: http
      port: 80
      targetPort: 8080
